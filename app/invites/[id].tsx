import { NoteText, ScreenContainer, ScreenTitle } from '../../src/components/ui/primitives';

export default function InviteDetailsRoute() {
  return (
    <ScreenContainer>
      <ScreenTitle title="Invite Details" subtitle="Phase 3 planned" />
      <NoteText>Detailed invite status and responses will be available in the next phase.</NoteText>
    </ScreenContainer>
  );
}
