import { NoteText, ScreenContainer, ScreenTitle } from '../../src/components/ui/primitives';

export default function PartnerSharedRoute() {
  return (
    <ScreenContainer>
      <ScreenTitle title="Shared Experiences" subtitle="Phase 2 planned" />
      <NoteText>Shared event feedback comparison and shared feed arrive after partner linking.</NoteText>
    </ScreenContainer>
  );
}
