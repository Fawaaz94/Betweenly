import { NoteText, ScreenContainer, ScreenTitle } from '../../src/components/ui/primitives';

export default function PartnerLinkRoute() {
  return (
    <ScreenContainer>
      <ScreenTitle title="Partner Link" subtitle="Phase 2 planned" />
      <NoteText>Partner invitation and link acceptance flow will be added in the next milestone.</NoteText>
    </ScreenContainer>
  );
}
