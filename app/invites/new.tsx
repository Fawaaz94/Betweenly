import { NoteText, ScreenContainer, ScreenTitle } from '../../src/components/ui/primitives';

export default function NewInviteRoute() {
  return (
    <ScreenContainer>
      <ScreenTitle title="Plan Invite" subtitle="Phase 3 planned" />
      <NoteText>Planned intimacy invites and optional calendar sync are queued for phase 3.</NoteText>
    </ScreenContainer>
  );
}
